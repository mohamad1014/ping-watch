#!/usr/bin/env bash
set -euo pipefail

scripts_dir=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
repo_root=$(cd "$scripts_dir/.." && pwd)

(
  cd "$repo_root/e2e"
  PING_WATCH_E2E_BACKEND_URL=http://127.0.0.1:8002 npm test
)
